#include<stdio.h>
int main()
{
    printf("this program tells you which number is the smallest after you enter each number\n");
    int num1,num2,num3;
     printf("enter number 1=");
     scanf ("%d",&num1);
     printf("enter number 2=");
     scanf("%d",&num2);
      printf("enter number 3=");
     scanf ("%d",&num3);

     if(num1<num2)
            {if (num2<num3)
              printf("number 1 is the smallest");}
    if(num1>num2)
            {if (num2>num3)
              printf("number 3 is the smallest");}
    if(num1>num2)
            {if (num2<num3)
              printf("number 2 is the smallest");}

              return 0;
}
